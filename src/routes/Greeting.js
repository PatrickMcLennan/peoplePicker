import React from 'react';
import { greetingCSS as css } from '../style';

const Greeting = () => (
  <section style={css.section}>
    <h2 style={css.h2}>View your current people or find new people to pick!</h2>
  </section>
);

export default Greeting;
